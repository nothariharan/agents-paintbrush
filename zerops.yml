zerops:
  - setup: web
    build:
      base: nodejs@22
      buildCommands:
        - npm install
        - npm run build
      deployFiles:
        - .output
        - package.json
        - package-lock.json
        - node_modules
        - public
      cache:
        - node_modules
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      start: npm run start
      envVariables:
        PORT: "3000"
        DATABASE_URL: ${db_connectionString}

  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - npm --prefix ./api install --omit=dev
      deployFiles:
        - api
      cache:
        - api/node_modules
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      start: node api/server.mjs
      envVariables:
        PORT: "3000"
        DATABASE_URL: ${db_connectionString}
