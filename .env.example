# Local only — copy to `.env.local` (not committed).
# Points the browser at the Zerops API so waitlist hits Zerops Postgres.
VITE_PUBLIC_API_BASE=https://api-2b3c-3000.prg1.zerops.app

# Optional local server routes — needs Zerops VPN (`zcli vpn up`).
# DATABASE_URL=postgresql://db:PASSWORD@db:5432/db
PORT=3000
